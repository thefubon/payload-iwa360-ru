# =============================================================================
# DATABASE
# =============================================================================
# PostgreSQL connection string (Neon, Railway, Supabase, etc.)
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URI=postgresql://user:password@host:5432/database

# =============================================================================
# PAYLOAD CMS
# =============================================================================
# Secret key for Payload CMS (generate a long random string)
# Используйте: openssl rand -base64 32
PAYLOAD_SECRET=your-super-secret-key-here

# Public URL of your site (for email links and metadata)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# =============================================================================
# VERCEL BLOB STORAGE (для хранения файлов)
# =============================================================================
# Vercel Blob Storage token
# Получить: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=vercel_blob_token_here

# =============================================================================
# EMAIL (RESEND)
# =============================================================================
# Resend API Key для отправки email
# Получить: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx

# Email адрес отправителя (должен быть верифицирован в Resend)
# Формат: noreply@yourdomain.com или Name <noreply@yourdomain.com>
RESEND_FROM_EMAIL=noreply@example.com

# =============================================================================
# BASIC AUTH (для защиты админки на production)
# =============================================================================
# Включается только в production (NODE_ENV=production)
# Оставьте пустым для отключения базовой аутентификации
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=your-secure-password

# =============================================================================
# NEXT.JS
# =============================================================================
# Environment (development | production | test)
NODE_ENV=development

# =============================================================================
# ПРИМЕЧАНИЯ
# =============================================================================
# 
# 1. DATABASE_URI:
#    - Обязательная переменная для работы с PostgreSQL
#    - Рекомендуется использовать Neon (https://neon.tech)
#    - В production обязательно используйте SSL (sslmode=require)
#
# 2. PAYLOAD_SECRET:
#    - Критически важно! Используйте длинный случайный ключ
#    - НЕ ДЕЛИТЕСЬ этим ключом публично
#    - Генерация: openssl rand -base64 32
#
# 3. BLOB_READ_WRITE_TOKEN:
#    - Для хранения загружаемых файлов (изображения, документы)
#    - Создается автоматически при подключении Vercel Blob Storage
#
# 4. RESEND (Email):
#    - RESEND_API_KEY: получите на https://resend.com/api-keys
#    - RESEND_FROM_EMAIL: должен быть верифицирован в Resend
#    - Без этих переменных email будет выводиться в консоль
#    - Подробнее: см. _docs/EMAIL_SETUP.md
#
# 5. BASIC_AUTH (опционально):
#    - Защищает админку /admin/* на production
#    - Работает только когда NODE_ENV=production
#    - Для development не требуется
#
# 6. NEXT_PUBLIC_SERVER_URL:
#    - Используется для генерации абсолютных ссылок в email
#    - В production замените на ваш реальный домен (https://example.com)
#
# =============================================================================
